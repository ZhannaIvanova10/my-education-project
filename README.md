# 🎓 My Education Project - Django + Docker

## 📋 Описание проекта
Учебный проект на Django с Docker, PostgreSQL, Redis и Celery.

## 🚀 Быстрый старт (Разработка)

### Требования
- Docker и Docker Compose
- Git

### Запуск проекта
```bash
# 1. Клонировать репозиторий
git clone https://github.com/ZhannaIvanova10/my-education-project
cd my-education-project

# 2. Создать .env файл из примера
cp .env.example .env
# Отредактируйте .env файл (укажите свои настройки)

# 3. Запустить проект
docker-compose up --build

# 4. Открыть в браузере
http://localhost:8000
🐳 Docker Services
backend: Django приложение (порт 8000)

postgres: PostgreSQL база данных (порт 5432)

redis: Redis для кеша и Celery (порт 6379)

celery_worker: Celery воркер для асинхронных задач

celery_beat: Celery beat для периодических задач

⚙️ Makefile команды
bash
make up           # Запуск всех сервисов
make down         # Остановка всех сервисов
make logs         # Просмотр логов
make migrate      # Применение миграций
make superuser    # Создание суперпользователя
make shell        # Запуск shell в контейнере
make status       # Статус всех сервисов
🚀 Деплой на продакшен сервер
Шаг 1: Настройка сервера
Создайте VPS сервер (Ubuntu 22.04 или Debian 12)

Подключитесь к серверу:

bash
ssh root@ваш-ip-адрес
Установите необходимое ПО:

bash
apt update && apt upgrade -y
apt install -y docker.io docker-compose nginx git
Создайте пользователя для деплоя:

bash
adduser deployer
usermod -aG sudo deployer
usermod -aG docker deployer
Шаг 2: Настройка проекта на сервере
bash
# Подключитесь как пользователь deployer
ssh deployer@ваш-ip-адрес

# Клонируйте репозиторий
git clone https://github.com/ZhannaIvanova10/my-education-project
cd my-education-project

# Создайте .env файл для продакшена
cp .env.production.example .env
nano .env  # Отредактируйте файл
Шаг 3: Настройка GitHub Actions
В репозитории GitHub перейдите: Settings → Secrets and variables → Actions

Добавьте следующие Secrets:

SERVER_IP: IP адрес вашего сервера

SSH_PRIVATE_KEY: Приватный SSH ключ для доступа к серверу

SSH_USERNAME: Имя пользователя (deployer)

Шаг 4: Ручной деплой (если нужно)
bash
# На сервере выполните:
cd /home/deployer/my-education-project
./deploy.sh
Шаг 5: Автоматический деплой через GitHub Actions
При каждом push в ветку main автоматически:

Запускаются тесты
Если тесты проходят успешно - происходит деплой на сервер
Приложение становится доступным по адресу: http://ваш-ip-адрес:8000
🛠️ Troubleshooting (Решение проблем)
Проблема: Приложение не запускается
bash
# Проверьте логи
docker-compose logs

# Проверьте статус контейнеров
docker-compose ps

# Пересоберите контейнеры
docker-compose up -d --build
Проблема: Ошибки базы данных
bash
# Примените миграции
docker-compose exec backend python manage.py migrate

# Создайте суперпользователя
docker-compose exec backend python manage.py createsuperuser
📞 Поддержка
Создайте Issue в репозитории GitHub

Или свяжитесь с автором проекта

📄 Лицензия
MIT

## CI/CD Pipeline

Проект использует GitHub Actions для автоматического тестирования и деплоя.

### Автоматические тесты
При каждом push в репозиторий или создании pull request:
1. Устанавливаются зависимости Python
2. Проверяется конфигурация Django
3. Выполняются тесты приложения

### Автоматический деплой
После успешного прохождения тестов при push в ветку main:
1. Приложение автоматически деплоится на сервер
2. Выполняются миграции базы данных
3. Собираются статические файлы
4. Перезапускаются сервисы


